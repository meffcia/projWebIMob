<ContentPage x:Class="OnlineShop.MauiClient.Views.CartView.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Label Content="Twój koszyk" FontSize="20" FontWeight="Bold" Margin="10" />

        <!-- Sekcja koszyka -->
        <StackPanel Margin="10,50,10,70">
            <DataGrid ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Product.Name}" />
                    <DataGridTextColumn Header="Cena" Binding="{Binding Product.Price}" />
                    <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" />
                    <DataGridTemplateColumn Header="Akcja">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!-- <Button Content="Usuń" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding ProductId}" /> -->
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Label Content="Kwota całkowita: " FontWeight="Bold" HorizontalAlignment="Left" />
                <Label Content="{Binding TotalPrice}" VerticalAlignment="Center" Margin="0,0,10,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Content="Wyczyść koszyk" Command="{Binding ClearCartCommand}" Margin="0,0,10,0" />
                <Button Content="Złóż zamówienie" Command="{Binding CreateOrderCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ContentPage>
